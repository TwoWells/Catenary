{
  "description": "Formats Catenary file tool output for readability — edit_file shows a colorized diff, write_file shows a numbered preview",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__.*__edit_file|mcp__.*__write_file",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/format_tool_output.py"
          }
        ]
      }
    ]
  }
}
